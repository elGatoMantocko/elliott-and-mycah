server {
  listen 8080;
  listen [::]:8080;

  gzip on;
  gzip_types *;

  server_name localhost mycahandelliott.com elliottandmycah.com;

  location / {
    root /var/www/html;
    index index.html;
    try_files $uri $uri/ /index.html;
  }
}