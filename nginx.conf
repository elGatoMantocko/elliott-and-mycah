server {
  listen 80;
  listen [::]:80;

  gzip on;
  gzip_types *;

  server_name localhost mycahandelliott.com elliottandmycah.com;

  location / {
    root /var/www/html;
    index index.html;
    try_files $uri $uri/ /index.html;
  }
}